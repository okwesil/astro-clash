{
    "name": "astro-clash",
    "private": true,
    "version": "0.2.0",
    "type": "module",
    "scripts": {
        "dev": "vite --host",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "postversion": "npm run build && firebase deploy && git push"
    },
    "devDependencies": {
        "typescript": "~6.0.2",
        "vite": "^8.1.0"
    },
    "dependencies": {
        "kaplay": "^3001.0.19",
        "peerjs": "^1.5.5"
    }
}
